{
  "rules": {
    "plugin/use-baseline": [
      true,
      { "available": "widely", "ignoreSelectors": ["nesting"] }
    ]
  },
  "plugins": [
    "stylelint-plugin-use-baseline"
  ],
  "extends": ["stylelint-config-standard"]
}
